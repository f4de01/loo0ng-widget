# loo0ng-widget 呈现层

桌面卡片的呈现层词汇。案件、图、模块、节点、状态、高亮、前方、时限、条目、案件根目录这些共享词一律以 loo0ng-skills 的 `CONTEXT.md` 为准，本文件只装卡片自己的词。2026-09-22 呈现定形那一轮建立。

## 页面

**清单页**：
卡片打开时的第一页，一案一行横着比：目录名、分段进度条与计数、当前模块与当前节点。
_Avoid_：列表页、首页、总览、看板、仪表盘

**案件页**：
在清单页选中一案后进入的第二页，只看这一案的节点，两种形式按钮切换；在同一扇窗里切换，不是新窗。不显示目录名。
_Avoid_：详情页、卡片页、案件详情、弹窗、子窗口

**模块视**：
案件页的形式之一：按模块分组的节点清单，模块行可折叠，节点前一枚圆圈表示状态。
_Avoid_：列表视图、树、大纲、任务清单

**矩阵视**：
案件页的另一形式：一格一节点，按图序列内自上而下、列间自左而右排；一列至多六格，一个模块排满六格就接着占下一列，新模块总是另起一列。格的尺寸固定，不随节点数伸缩，排不下就横向滚动。颜色表示状态，悬浮出概览。轴是图序，不是日期。
_Avoid_：热力图、时间轴、日历、贡献图、活动图、甘特图

## 内容

**当前节点**：
沿用路由的定义：已生成、等律师确认的节点。清单页只取图序第一个；一个都没有时那一格显示下一个（前方第一个），靠圆圈的实空区分，不用文字。
_Avoid_：进行中节点、活动节点、待办、任务

**分段进度条**：
一条按四种状态计数分段的细条，各段宽度比即计数比；清单页每行一条，案件页表头一条。
_Avoid_：完成率、百分比、进度环、进度圈

**圆圈**：
节点状态的图形编码，圆圈与矩阵格共用一套：未生成空心、已生成半实、已确认实心、不适用淡灰带斜线；高亮未清再加一圈琥珀色描边。琥珀是全卡唯一的暖色。
_Avoid_：复选框、勾、红绿灯、状态点

**悬浮概览**：
鼠标停在矩阵格上出现的自绘小浮层：模块 › 标题、状态与时间、时限、等你看（有则）。模块视里节点已经摊开，悬浮只补时限全文。
_Avoid_：tooltip、气泡、详情弹窗、title 提示

**时间**：
节点行右侧那个日期：已确认显示确认时刻，已生成显示生成时刻，未生成与不适用不显示。取引擎写在机器可读视图里的拍板时刻，不是文件修改时间；显示串（如「9月21日」，跨年才带年）由扫描脚本预先格式化，页面照印。
_Avoid_：修改时间、更新于、mtime、相对时间、几天前

## 窗口

**唤起**：
从卡片之外打开它的动作：宿主没开就连宿主一起拉起；卡片已经开着（含最小化）就什么都不发生，还原永远在任务栏或程序坞。三个入口（随宿主启动、桌面快捷方式、agent 一句话）都落到包里同一条命令。
_Avoid_：调用、召唤、聚焦、还原、置顶、带到前面

**呈现偏好**：
卡片记在 WebView 本地存储里的那几样：窗口位置、停在哪一页哪一案、选中的形式。它是硬边界 2 的唯一例外，不碰文件、不碰工作区。
_Avoid_：设置、配置、用户数据、缓存、状态持久化

**材质**：
卡片的底：透明窗上自绘的圆角浅色卡片，带细描边与柔投影；宿主允许时再向系统要一层真实模糊（Windows Acrylic、mac vibrancy），要不到就停在自绘那层。
_Avoid_：玻璃、毛玻璃（作为必须项）、皮肤、主题、背景图

**交互区**：
卡片上按住不拖窗的那些地方：按钮、案件行、模块行、节点行、矩阵格。其余一概是把手——透明外边距、顶行、空白、进度条、文字都能拖（#17）。原生可点的元素自己算数；页面自己画的行与格靠记号类 `.interactive` 进来，新加的行与格跟着标。
_Avoid_：热区、点击区、可点区域、拖拽把手（指反面）

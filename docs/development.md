# 开发与验证

实现规格是 [#2](https://github.com/f4de01/loo0ng-widget/issues/2)，母票是 [#1](https://github.com/f4de01/loo0ng-widget/issues/1)。原型分支只作资料，不合并。

Windows 首版已通过 #8 最终验收，呈现定形已通过 #20 验收，两轮的逐项证据、机器与版本、Python 3.9 跑法及现场恢复情况见 [Windows 验收记录](acceptance-windows.md)。下文保留各实现票当时的验证过程与后续补验。

## 包与扫描

`widget/` 是组件包。测试缝是随包脚本的命令行，共两条：`scan.py` 完成一层发现、读取和聚合，`launch.py` 只找宿主、调一条命令（见下面的 #15 一节）。只支持 Python 3.9 及以上和标准库，没有 Node、JS 测试跑器或类型检查器。

```powershell
python -m unittest discover -s tests -p 'test_*.py'
python -m compileall -q widget tests
python widget/scan.py --根 <合成案件根目录>
python widget/scan.py --设置 <设置文件>
```

`--根` 可重复，提供它就不读取设置。默认设置是 `~/.loo0ng/卡片设置.json`，形状为 `{"根目录":["绝对路径"]}`。

输出沿用 #2 的 `扫描时间`、`设置文件`、`根目录`、`行`、`读不出`；另加顶层 `案件数`（成功读出的案件数）和每行 `待看数`。两个计数由扫描脚本生成，页面不再计算数组长度。用户已确认这一契约补充。

#5 的案件排序由脚本完成：有待看、有前方、空图、没有前方四档；同档按系统 `zh_CN.UTF-8` 中文排序，目录名相同时再按路径固定顺序，不按生成时间。没有中文 locale 时明确报错，不静默退回 Unicode 序。Windows 的 Python 3.9.25 已验证；mac 的系统排序仍待交付验收。

清单页每行三层，行不再展开：目录名与「已确认 / 总数」、分段进度条、「当前模块 › 当前节点」。契约见下面的 #14 一节。

合成视图手写在 `tests/test_scan.py`，测试不调用引擎。四条边界测试分别检查包源码禁用名称，以及输出不含条目、不含文书路径、不含前方列表。检查禁用名称前移除允许的完整文件名，避免把其中的后缀误认成另一个文件名。

## 页面与宿主

发布版按 [README](../README.md#装法) 的一条命令安装；本地开发则把 `widget/` 的内容装到 `~/.glzr/zebar/loo0ng/`，通过 Zebar 的「案件卡片 / 默认」打开。已有安装先备份到 Zebar 目录外。

`zpack.json` 的 `includeFiles` 是资源服务器的白名单，不是附带清单。`htmlPath` 指向的文件未被覆盖时，宿主直接回 500。新增资源必须同时加入白名单：当前 `*.html`、`*.css`、`*.js`、`*.py` 覆盖包入口，`vendor/**` 覆盖完整依赖、manifest、说明和许可证。`zpack.json` 是宿主读取的包描述，不是页面请求的资源；安装时仍必须复制它。不要把本机生成的 `__pycache__/` 拷进包里。

页面直接按扫描数组顺序渲染。每五秒发起扫描，失败在顶部压一条横幅并保留上一轮画面；下一轮成功横幅即消失。没有手动扫描按钮，也不显示扫描时刻（#16），轮询用一个忙碌标志避免重叠执行。

未识别宿主时直接报错。不装宿主调样式时，在仓库根目录运行 `python -m http.server 8000 --bind 127.0.0.1 --directory widget`，浏览器打开 `http://127.0.0.1:8000/index.html?dev=1`，在页面里手动选择扫描 CLI 导出的 UTF-8 JSON。用完在终端按 Ctrl+C 停止服务器。这条显式通道不轮询，也不替代宿主验收；不带 `?dev=1` 会明确报未连接宿主。再加 `&blur=1` 把卡片底色切到系统模糊调上时的那一档（60%），只为在调不上模糊的平台上看一眼那层，宿主里不认这个参数。

只用合成案件生成预览 JSON。PowerShell 5.1 的 `>` 默认会写成 UTF-16，可在合成根已准备好后用下面的方式保存（将路径换成你的合成根）：

```powershell
$env:PYTHONIOENCODING = 'utf-8'
[Console]::OutputEncoding = [Text.UTF8Encoding]::new($false)
$preview = python widget/scan.py --根 'D:/合成案件'
if ($LASTEXITCODE -ne 0) { throw '扫描失败' }
New-Item -ItemType Directory -Force .scratch | Out-Null
[IO.File]::WriteAllText((Join-Path $PWD '.scratch/preview.json'), ($preview -join "`n"), [Text.UTF8Encoding]::new($false))
```

开发时每次改包后，先关闭该组件（缓存被占用就退出 Zebar），确认 `%APPDATA%\zebar\webview-cache\loo0ng` 是该包的缓存目录，再清理并重新打开。否则看到的可能还是上一版，包括上一版的报错页。只清 `loo0ng`，勿清其他组件缓存；以后记在 WebView 本地存储里的窗口位置也会随清缓存丢失。

客户端固定为 `zebar@3.3.1`，通过 `vendor/zebar-3.3.1.js` 本地导入；完整浏览器依赖、来源哈希和许可证均随包提供，`vendor/**` 纳入资源白名单。升级步骤见 [随包客户端](../widget/vendor/README.md)。一条命令安装属于 #6，窗口控件与拖动属于 #7，最终人手验收属于 #8。

## #23 矩阵视定形：六格一列、黄金比板、横向滚动

`CONTEXT.md`「矩阵视」的新定义先落了一笔（`ed49a8e`）；这一轮只动页面：`style.css` 重写矩阵那一段，`app.js` 的 `matrixView()` 换了一层结构，扫描脚本一个字没改，#19 给的 `模块[].节点[]` 就够。

**板**：矩阵放进一块 `.matrix`，底与清单页的胶囊同一种（`--capsule`、`--capsule-radius`），同样向外探出 `--capsule-pad`；`aspect-ratio: 1.618` 定宽:高，宽跟着卡片走，贴顶，下方留空。「板的宽 = 卡片内宽」按胶囊的宽算（比内容宽两侧各多 10px），与清单页的案件胶囊、模块视的模块胶囊对齐。板是尺寸容器（`container-type: size`），格边长 `--side` 由板高推出：`(100cqh − 上下内边距 − 模块名那一条) / 7`——六格加五个列内间距，列内间距是边长的 1/5，一共 7 个边长。模块间距是边长的 1/3。默认 440 宽下格约 30px、列内间距约 6px、模块间距约 10px；格只随律师拉窗口变，不随节点数、模块数变。`--side` 写在板上，在板里的元素上才代入，所以 `100cqh` 取的是板的高。原先的 `--cell-min` / `--cell-max` 一并去掉。

**列与格**：每个模块一组 `.group`，里面的 `.cells` 是固定 6 行、按列流的 grid（`grid-auto-flow: column`），满 6 格自动接到下一列，新模块另起一组。列数是布局得出的，页面不数。空模块仍是 `.cells:empty::before` 那枚虚线格，它在 grid 里占第一格。

**模块名**：横排在那几列上方、跨列，绝对定位，宽就是那几列的宽，不撑宽模块；超出省略，悬浮见全名。只占一列的模块不写名，由 `.group:has(.cell:nth-child(7))` 决定显隐——满 7 格才会占第二列，这是选择器，不是页面在数；名字仍在格的悬浮概览里（「模块 › 标题」）。名字那一条对所有模块都留着高，各组的格顶端对齐。

**横向滚动**：板里一条 `.lane` 横向滚，页不滚；只靠原生手势（触控板双指、Shift+滚轮），竖向滚轮不改成横向。不给滚动条：按住它会被全卡可拖（#17）当成把手拖窗，而且它一出现就吃掉板高。右缘渐隐是滚动驱动的动画（`animation-timeline: scroll(self inline)`）推一个注册过的 `--more`，遮罩宽度（`--fade`，1.5 个边长）随它走：排得下时滚动时间线不生效、`--more` 停在初值 0，不淡；有得滚时一直是 1，`animation-range` 只把离滚到底还剩一个 `--fade` 的那段路交给动画，在那段里收到 0，最后一列正好随之露全。溢出不足一个 `--fade` 的案子一开头就是半淡。整段包在 `@supports (animation-timeline: scroll())` 里，引擎不认就不淡，滚动照常——WebView2 认；mac 的 WKWebView 要 Safari 26 这一代才认，旧系统上没有渐隐，由 mac 交付票看。

- Python 3.9.25：全套 45 条 unittest 通过、零 skip；`compileall` 与 `git diff --check` 通过。本票没动随包脚本，没有新增机械测试（页面行为不做机械测试，不变量 4）；源码禁用名、色值只住在变量表两条照旧覆盖（遮罩那一端的不透明色也进了变量表，叫 `--opaque`）。
- 写的时候在临时目录里用 #20 那五案的扫描输出，另手加一案（模块分别 1、6、7、13、0、12、2 个节点），在无头 Edge 里把页面副本量过：1、6、7、13 个节点的模块分别占 1、1、2、3 列，空模块一列虚线格；3 个模块与 30 个模块的格边长相同，拉宽窗口格随之放大、板仍 1.618；30 个模块时只板内横滚、页面不横向溢出，`--more` 在开头、中段、离底 60px 都是 1，离底 20px 约 0.38，滚到底为 0，排得下的那案始终为 0；单列模块名隐藏、多列的显示并省略；点格仍切到模块视、展开那个模块、那一行闪。**那是探查，不是验收凭据**，副本里的驱动代码不提交。
- 2026-09-25 宿主里看过（Windows Zebar 3.3.1，律师授权），**比例没过**：同 #22 的做法，备份原包与卡片设置，装上本版、清 `loo0ng` 的 WebView 缓存，设置临时指向 #20 那五案，用 ADR-0001 那条命令开卡片。律师看了矩阵视，不满意黄金比板推出来的格（默认宽下约 30px）。随后照律师给的参考图（一张 150% 缩放下截的贡献图式网格）试了一版定死的尺寸：格 14px、格缝 3.5px、格圆角约 14%、板内边距 12px，板高包住格、不再定 1.618、格不随窗宽变；同步进包重开后律师看过，**仍不对**。律师定：本票就此收尾，矩阵视另开一轮 grill 重新设计。那版试验没提交，补丁留在忽略目录（`.scratch/issue23-fixed-size.patch`），仓库里保持本票提交的那一版。其余几项（横滚手势、渐隐、模块名、五态与琥珀圈、悬浮概览、点格定位、窗拉宽拉矮时板跳不跳）这一轮没有逐项验，留给新设计那一轮。
- 验后把包同步回仓库这一版（全包哈希与仓库一致）、清 `loo0ng` 的 WebView 缓存，按字节哈希恢复卡片设置（`842cb9d0…`，前后一致），宿主照验收前的样子重新开着。保留新版组件包，原包与设置备份留在忽略目录（`.scratch/issue23-backup`）。

## #22 冷色调与胶囊行

页面上只动 `style.css`，页面脚本与扫描脚本一个字没改；另加一条包测试（见下）、给 ADR-0004 补附注。

**色值**：`:root` 整张表换成冷色调（ADR-0004 的 2026-09-25 附注）：底 `235, 240, 245` 的浅蓝灰，字仍是深色三级；状态色同一蓝色系——已确认 `--confirmed` 深蓝、已生成 `--generated` 中蓝、未生成 `--unstarted` 灰、不适用 `--na` / `--na-slash` 淡灰斜线；琥珀不动。形状编码不变，圆圈、矩阵格、进度条共用这几个变量：已生成的圆圈与格原先是深绿的半填，现在用 `--generated` 半填，与进度条上那一段同色。报错的红棕保持不变（律师看过后定的）。三级字在底色上的对比度约 12.6 / 6.2 / 4.8，最小 11px 那级也过 4.5。

**散落的色值收进表里**：卡片的描边与内高光、浮层投影、mac 红绿灯三色原先直接写在规则里，现在都是 `:root` 的变量（红绿灯照系统的样子，不随色调走）。`tests/test_package.py` 多一条：去掉注释后，`:root` 之外不许出现 `#hex` 或括号里直接写数字的 `rgb()` / `hsl()`；`rgba(var(--card-rgb), …)` 这种不算。它守的是 ADR-0004「颜色全抽成变量」，下回换色调只换那一张表。

**胶囊**：清单页每案一条 `.case-row` 胶囊，半透明白底（`--capsule`，悬浮时 `--capsule-hover`）、圆角 `--capsule-radius` 12px、胶囊之间 `--capsule-gap` 6px，原先的分隔线与 `--line-soft` 一并去掉。胶囊向外探出 `--capsule-pad`（10px，由 `--pad`、两层圆角算出），离卡片边剩卡片圆角 − 胶囊圆角 = 6px，贴近卡片圆角时两段弧同心；内边距补回同样多，所以字仍落在 16px 那条线上，节点行的缩进不用动，圆圈照旧对齐在模块标题下面。模块视的 `.module-head` 做成同款胶囊，模块之间同样 6px，节点行不做胶囊、挂在它下面；折叠、`aria-expanded`、焦点框（画在胶囊内侧，随圆角走）都照旧。读不出那一行随案件改成同样的形，底仍是暗的。交互区没变：案件胶囊仍带 `.interactive`，模块行仍是原生 `button`，拖卡片的规则一行没动。胶囊之间那 6px 缝与两侧 6px 的边不在交互区里，按下去拖卡片——照 #17「整张卡片减去交互区都是把手」，这是规则的本意，不是例外。

- 琥珀圈与图形之间那 1.5px 的缝仍取卡片底色：在胶囊上它比胶囊略深一点，截图里看不出来，没为它另开变量。
- 写的时候在临时目录里拿五个种子回放的合成工作区（另手加一条读不出）的扫描输出，用无头 Edge 把页面副本定成 440×620，截了新旧两版的清单页、模块视、矩阵视拼成一张对照图。2026-09-25 律师看过，认可这套色值，并定报错保持红棕。副本里的驱动代码与截图不提交。
- 2026-09-25 宿主里看过（Windows Zebar 3.3.1，律师授权）：同 #20 的做法，备份原包与卡片设置，装上本版、清 `loo0ng` 的 WebView 缓存，设置临时指向 #20 那五个种子回放的合成工作区，用 ADR-0001 那条命令开卡片。清单页五条胶囊、蓝色系的圆圈与进度条、琥珀圈，模块视的模块胶囊与挂在下面的节点行，矩阵视的格与列名，在 WebView2 里都和开发预览一致。之后把胶囊几何抽成 `--capsule-pad`，无头 Edge 里前后三张截图逐像素相同，再把这份样式同步进包、核对全包哈希与仓库一致。验后按字节哈希恢复卡片设置（前后一致），宿主重开；保留新版组件包，原包与设置备份留在忽略目录。截图只截卡片窗口那块矩形，不进仓库。

## #19 矩阵视、悬浮概览、点格定位

案件页表头右侧多了「模块」「矩阵」两枚并排按钮，当前那个填色（`aria-pressed`），点了切形式并写 `loo0ng.form`——它现在有 `module`、`matrix` 两个合法值，重开卡片回到上次的形式。同样只动页面三件，扫描脚本一个字没改：矩阵要的列、格、状态、高亮、时限、时间显示串，#14 的 `模块[].节点[]` 已经全给了，空模块也照样出（`节点: []`，有测试守）。

**矩阵视**（排布与尺寸已由 #23 改掉，见上；这里留作当时的记录）：一列一模块、列内一格一节点，都照扫描输出的次序摆，页面不排。尺寸全在样式表里：每列 `flex: 1 1 0`，均分卡片内宽，一份就是「内宽 ÷ 模块数」，再被 `min-width` / `max-width` 夹在 `--cell-min`、`--cell-max` 的 1.2 倍之间；列是容器（`container-type: inline-size`），格边长取这一份的 5/6（`100cqw / 1.2`）、间距取 1/6，于是间距正好是边长的 1/5，页面不数模块、不算尺寸。用物理的 `cqw` 不用 `cqi`：竖排的列名里 `cqi` 落到另一根轴上，列名会被撑成几百像素宽。模块多到一份不足 12px（默认宽度下约 32 个模块）时格停在 10px，案件页横向滚动。
- 格子颜色与圆圈共用一套类（`.dot` 与 `.cell` 同一组规则，只是形状不同；页面上同一个 `encode()` 给两者上类）。琥珀圈在格上随边长缩放（约 1–2.6px）：圆圈那一档 3px 比最小间距还宽，会压到邻格上。
- 空模块的虚线格是 `.cells:empty::before`，样式表按「列里没有格」画，页面不判空。
- 列名竖排（`writing-mode: vertical-rl`）放列底，框长 6em、超出省略；各列被 flex 拉成一样高、列名框一样长，所以列名顶端对齐。全名在自绘浮层里。

**悬浮概览**：停在格上出自绘浮层，四行——模块 › 标题；状态 · 时间显示（没有时间显示就只印状态）；时限（有才印）；「等你看」（高亮未清才印，与画琥珀圈同一个判据，就是引擎给的 `高亮` 字段）。移开即消失，不用原生 `title`。模块视里节点行仍只在时限上补全文。

**点格定位**：点一格切到模块视（和按「模块」按钮一样写下 `loo0ng.form`，重开卡片停在模块视——停在哪种形式就记哪种）、那个模块记成展开（和用户手动点开的一样，记在这一次里）、那个节点行摆到案件页中间、底色闪一次（600ms 的 `flash` 动画）；`prefers-reduced-motion` 下样式表不放这段动画，只定位。第几个模块、第几个节点是这一格在输出里的位置，模块视照同一份输出同一个次序画，按位置就找得到。格标 `.interactive`：按住不拖窗。

- Python 3.9.25：全套 44 条 unittest 通过、零 skip；`compileall` 与 `git diff --check` 通过。本票没动随包脚本，缝没变宽，没有新增机械测试（页面行为不做机械测试，不变量 4）；源码禁用名那条照旧覆盖三个页面文件。
- 写的时候在临时目录里用合成根（一案 3 个模块、一案 30 个模块，含空模块、四种状态、未清高亮、长时限）的扫描输出，在无头 Edge 里把页面副本定成 440×620 量过：3 个模块时格 26px，30 个模块时格约 10.7px、最右一列的右边正好落在卡片内宽的右边，案件页不横向溢出；空模块的伪元素是虚线；点格后形式记成 `module`、那个模块展开、那一行带上 `flash`、浮层收起。**那是探查，不是验收凭据**，副本里的驱动代码不提交。
- **宿主验收并到 #20**「案件页」一条：两按钮切换与重开记忆、矩阵排布与列名（含竖排在 WebView2 里的样子）、悬浮概览四行与移开消失、点格定位与闪、系统减少动态下只定位不闪。

## #18 案件页、导航、模块视、呈现偏好

清单页点一案进案件页，表头的返回回清单页。只动页面三件（`index.html`、`app.js`、`style.css`），扫描脚本一个字没改——#14 交出来的字段已经够案件页照印：`模块[]` 的标题、状态、进度，节点的标题、状态、高亮、时限、时间与显示串，每案的 `路径`。

**两页一窗**：顶行与正文各是一条两屏宽的轨道（`#tops`、`#bodies`），卡片上一个 `.on-case` 把两条一起推过去半条，180ms；`prefers-reduced-motion` 下不滑。横幅夹在两条轨道之间，两页共用，所以两页的顶行都照旧给窗口按钮让出那一角。不在眼前的那一页设 `inert`：Tab 走不进去，悬浮也碰不到。启动时回到记住的那一页带一个 `.still`，两帧后摘掉——打开卡片就该停在那里，不是看它从清单页滑过去。

**案件页**：表头只有返回、与清单页同一个 `segmentedBar()` 画的分段进度条、「已确认 / 总数」；不印目录名、不放当前模块行，「模块 / 矩阵」按钮归 #19。正文按形式查 `VIEWS` 表出，今天只有 `module`。模块行是原生 `button`（带 `aria-expanded`），本来就在交互区里；节点行点了无事，但按 #17 的约定标了 `.interactive`，按住它不拖窗；清单页的案件行现在点得动，另给了 `tabindex` 与回车 / 空格。

**模块视**：模块状态为「进行中」的默认展开，「已完成」「不适用」折叠，允许多开；模块行三样是标题、状态（三态各一个类）、按模块的计数。节点行四段：圆圈 | 标题 | 时限 | 时间。标题占它要的宽、挤了才省略，时限吃剩下的、一行省略，鼠标停在时限上出自绘浮层见全文（不用原生 `title`）；时间只印扫描给的 `时间显示`、ISO 原串放进 `<time datetime>`，扫描按状态给或不给，页面不判。空模块展开了也什么都不画，模块行的「0 / 0」已经说了。

**页面仍不算**：进哪一案是按工作区路径在这一轮的 `行` 里查（查找，不是挑选，路径在一轮里不重）；默认展开看的是引擎给的模块状态；计数全是扫描脚本数好的。

**呈现偏好**（ADR-0002）：本地存储在窗口位置之外只多两项——`loo0ng.place` 记停在哪一页哪一案（`{"page":"list"}` 或 `{"page":"case","path":"<工作区路径>"}`），`loo0ng.form` 记选中的形式。启动时同步读、记住的赢；缺失、不是 JSON、形状不对、不可读，一律回到清单页与模块视。`loo0ng.form` 今天只有 `module` 一个合法值，进案件页时连同 `loo0ng.place` 一起写下；#19 有了按钮后切换时也写它。用户点开、合上了哪些模块只记在这一次里，不进本地存储——呈现偏好就那三样。

**轮询与那案消失**：案件页每轮只在这一案的字段真变了时重画（拿这一行的 JSON 串比上一次），所以五秒一轮不打断展开状态、滚动位置和正停着的浮层；真重画时展开状态照「用户动过的按用户的、没动过的按模块状态」重放。那案这一轮不在输出里（读不出、被挪走），案件页正文清空，表头照 #18 验收条款的字面保留——返回键与上一次画的进度条、计数都留着，不提示、不自动回清单页；那案回来就原样画回来。启动时还没扫过第一轮，表头也还没有可画的，只有返回。设置没填好时一案都没有，案件页正文印那段可照做的指引——#12「设置错误整页只显示指引」，它不能藏在 inert 的清单页里等律师自己点返回去找。

- Python 3.9.25：全套 44 条 unittest 通过、零 skip；`compileall` 与 `git diff --check` 通过。本票没动随包脚本，缝没变宽，没有新增机械测试（页面行为不做机械测试，不变量 4）；源码禁用名那条照旧覆盖三个页面文件。
- 两个页面模块用 `node --check` 在临时目录的副本上解析过（仓库不引入 node）。
- 写的时候在临时目录里拿合成根的扫描输出，用无头 Edge 走过 `?dev=1` 的页面副本探路：截图看两页与折叠，另在同一个浏览器配置里跑两轮看「记住的页与案重开即回」「字段变了重画后折叠还在」「那案消失清空、回来复原」「存储是坏 JSON 回清单页」。**那是探查，不是验收凭据**：副本里加的驱动代码不提交；无头 Edge 的视口比截图宽，要把 `body` 定成 440×620 才看得到右边。
- **宿主验收并到 #20**（2026-09-22 裁定，#18 据此关闭）：进出页与横滑、系统减少动态下不滑、表头、展开折叠与多开、节点行四段、时限悬浮、重开回到上次的页与案、清缓存回清单页、挪走清空而表头保留、设置出错时案件页印指引，逐项在 #20 的「案件页」「呈现偏好」两条里看。改包后照「页面与宿主」那条清 `%APPDATA%\zebar\webview-cache\loo0ng` 再开，清缓存会连记住的页与案一起丢掉，那正是「清缓存后回到清单页」那一项要看的。

## #17 全卡可拖

把手从顶行换成整张卡片减去交互区（#12 的窗口裁定，接在 #7 的拖动实现上；#7 的验收记录在下面那节，行为的底子没动）。

**能拖的和不能拖的**：`window.js` 顶上一行 `INTERACTIVE = 'button, input, label, .win-controls, .interactive'` 就是交互区的全部定义。按下时 `event.target.closest(INTERACTIVE)` 命中就当没看见，别的地方一律能拖——透明外边距、顶行、空白、进度条、文字都在「别的地方」里。名单只列卡片今天真有的可点元素：窗口按钮，以及它那一角整块（`.win-controls`——两枚按钮之间那 2px 的缝和容器的留白按下去也不该拖窗，#7 挡的就是这一整块），开发预览那个文件选择器；页面自己画的行与格没有原生语义，靠一个记号类 `.interactive` 进来，今天只有清单页的案件行，**#18 的模块行、节点行与 #19 的矩阵格加上去时记得一并加 `.interactive`**，不然按住它们会拖窗——本仓库不引入 JS 测试跑器，这条约定没有机械守门，只有这行字和 `CONTEXT.md` 的「交互区」一条。行本身若做成原生 `button`，它自己就在名单里，不必再标。

**阈值**：按下先只记一笔（`press`），窗一格都不动；位移过 4 CSS 像素才转成真拖（`drag`），这时才要指针捕获、才开始调 `setPosition`。没过阈值就松手，什么都没发生过，`click` 照常落到按下的那个元素上——所以按下不调 `preventDefault`（它会连带吃掉后面的 `click`）。真拖起来时窗口原点取此刻的位置、位移仍从按下那一点算起，阈值那几像素不丢，全程 1:1 跟手。

**捕获落在 `<html>` 上**：整卡可拖之后按下的那个元素可能是案件行里的一段文字，而清单每五秒重绘一次，按 #7 那样把捕获挂在按下的元素上会在拖到一半时连元素带捕获一起没掉。指针事件因此听在 `window` 上（透明外边距也在里面），捕获挂在 `<html>` 上；`setPointerCapture` 要不到就停在没有捕获的那一档，不影响这一次拖动。失焦、指针取消、按键在窗外松开（下一次 `pointermove` 的 `buttons` 已经是 0）仍然就地结束，最小化与关闭前仍 `settle()` 结算最后一次移动，位置仍只在卡片自己移动成功后写本地存储。

**选中与光标**：全局 `user-select: none` 沿用 #12 的裁定，只给 `input, textarea` 开了例外（开发预览那个选择器，以及日后真出现的输入框）；这条例外不动「设置指引与横幅复制不出来」那一条，要开得先改 #12 的裁定。`touch-action` 全局给 `none`，触屏上一划才不会被当成滚动手势把拖动 cancel 掉；它一给就是整棵树——effective touch-action 要跟祖先求交，`.page` 上再写 `pan-y` 也压不回来，所以触屏上的页内滚动一并关掉了（默认尺寸下三案不溢出，本来也滚不着；鼠标滚轮不走这条路，不受影响）。哪天页内真要触屏滚动，得给把手单独铺一层，不是在 `.page` 上加一行。平时不给 `grab` 光标：能拖的是整张卡，一进窗就换手形反而吵；真拖起来才 `grabbing`（`html.dragging`，规则放在样式表末尾——它和 `.win-controls button` 的 `cursor` 特异度相同，写在前面会被那条盖掉）。顶行的 `id="top"` 随把手一起没了用处，删掉。

- Python 3.9.25：全套 44 条 unittest 通过、零 skip；`compileall` 与 `git diff --check` 通过。本票没动随包脚本，不变量 4 的那条缝没变宽，也没有新增机械测试——页面行为仍不做机械测试（`tests/test_package.py` 的开头写着同一条）。
- 两个页面模块另用 `node --check` 在临时目录的副本上解析过一次（仓库不引入 node，也不引入 JS 测试跑器）。
- 写的时候在临时目录里用 node 搭过一次性的架子探路（最小 DOM 替身 + 假的宿主窗口，走一遍微动当点击、过阈值跟手、交互区不拖窗、窗外松手与失焦结束这几条分支）。**那是探查，不是验收凭据**：架子不提交、不可复现，仓库不引入 JS 测试跑器（不变量 4），拖动仍以真宿主里的人手验收为准。
- **人手那半并到 #20**（2026-09-22 裁定：#17 据此关闭，真宿主里若发现不跟手或误拖另开票，不回头重开）。要在真宿主里看的：Windows 150% 缩放下按住外边距、顶行、空白、进度条、文字拖动是否 1:1 跟手；按住案件行与窗口按钮拖动窗口不动；按下微动后松手当点击（今天案件行还没有点击行为，先看窗口不动、窗口按钮照常生效）；全卡选不中文本；拖到一半赶上一轮重绘不断手；拖动中按住不放时页内滚动条那几像素按下去的是滚动条还是拖窗（Chromium 一般不把原生滚动条的按下发给页面，看一眼确认）。
- 三处已知取舍，一并在真宿主里看一眼：过阈值那一瞬窗口会一次补上按下以来的位移（约 4px），这样抓点始终贴着指针，代价是起手有一跳；`devicePixelRatio` 在起拖时取一次快照，跨不同缩放的显示器拖动会失真（#7 就是这样，整卡可拖之后更容易撞上）；触屏上的页内滚动按上面那条被关掉了。这三条都不在 #17 的验收条款里，遇到再开票。

## #16 外壳与包：材质、尺度、变量、错误现身、去页脚、去 luxon

壳按 ADR-0004 与原型票（#13）的变量表重做，页面仍只有清单页（案件页归 #18）。

**材质**：`zpack.json` 的 `transparent` 打开，默认预设 440×620。页面自绘卡片：`.card` 占满 `inset: var(--margin)`，圆角 18px、底色 `rgba(var(--card-rgb), var(--card-alpha))`、细描边 + 内高光 + 柔投影，投影落在那圈 12px 透明边距上。边距仍是页面的一部分，点得到、拖得动，不穿透到桌面。系统模糊按 ADR-0004 的裁定接在 `window.js` 的 `MATERIAL` 表上：调上了才把 `html[data-blur="1"]` 打开、底色从 90% 降到 60%，抛错就一言不发停在自绘层，只试一种效果，不按「哪个成功用哪个」去探。一个平台一行：**Windows 没有行**——Acrylic 会把整个窗口矩形连同圆角外的四个角磨砂成一块方板，#13 看过截图后裁定不要，所以 Windows 上一次都不调，卡片停在 90% 的自绘层。**mac 那一行是 `underWindowBackground` + `state: 'active'` + 跟着 `--radius` 走的圆角**：这一层是「窗内容背后那一层」，随系统外观走，radius 让磨砂贴住卡片的圆角而不从圆角外冒出来，`active` 是因为这张卡常驻桌面、基本不是活动窗，材质不能跟着失焦一起淡掉。**这一行尚未在 mac 实机上看过**：按 ADR-0004 同一条标准，若它同样盖满整个窗口矩形、或压不住浅色卡片，由 mac 交付票把这一行删掉（删掉即回到自绘那层，别处不用动）；`setEffects` 不抛错但没效果的情形页面无从得知，只能靠一行只试一种把它压到最小。Windows 上 60% 那一档只有 `?dev=1&blur=1` 的模拟能看到，那条路还会自己加一层 `backdrop-filter` 充当桌面模糊，真调上系统模糊时页面不叠这一层。

**变量**：颜色、间距、圆角、字号全在 `style.css` 的 `:root` 里，取值来自原型票的 A 纸那一套（含五态编码与进度条四段的配色；#22 起色值换成冷色调，见那一节）；字体栈补上 `-apple-system, "PingFang SC"`；正文 13px、次要 12px、最小 11px；不做深色模式，以后改的是这一组值。

**尺度与滚动**：卡片内顶行固定、`.page` 页内滚动，窗不自动长高；滚动条用 `scrollbar-width: thin` 加透明的 `scrollbar-color`，悬浮才把滑块染出来，不占一条常驻的灰边。照抄原型的做法，代价说清楚：Chromium 的 `thin` 是经典滚动条，内容真溢出时它仍占那几像素的宽，只是平时被染成透明，不是内容从它底下穿过的那种覆盖式。默认尺寸下三案不溢出，一条滚动条都不出现（#10 顺带看到的那条常驻灰边就是这么没的）；要不要换成自绘的覆盖式滑块，留给 #20 在真窗口里看过再说。窗口按钮平时 `opacity: 0`，鼠标进卡片或键盘走到才显，Windows 右上、mac 左上，顶行给它那一角留出内边距。

**错误现身**：设置错误时顶上的计数一并收起，整页只剩那段可照做的指引；读不出仍排在案件之后，暗色行只写目录名与原因；一轮扫描失败在顶部压一条横幅，下一轮成功即消失，中间画面保留。页脚、扫描时刻、「扫一次」按钮都去掉了，`?dev=1` 的开发预览通道留着。

**拖动**：顶行是这一版的把手（原来是 `<header>`，它随页脚一起没了）。整卡可拖是 #17，那票会把把手换掉。

**去 luxon**：esm.sh 的 zebar bundle 把 luxon 的 10 个模块外部化后逐一导入，9 个没人用，第 10 个只被 zebar 的 `date` provider 用一句，而这张卡片不用 provider。删掉那 10 条导入与对应文件、许可证、manifest 条目，`date` provider 那一句改成抛错说明；随包 SHA-256 重算记进 manifest，上游那份不动。细节与升级时的注意见[随包客户端](../widget/vendor/README.md)。包内文件从 34 个减到 23 个。

**新的测试文件**：`tests/test_package.py` 把原先人手跑的那串检查落成测试——白名单覆盖每个包内文件、包内文件名全 ASCII、manifest 条目与随包字节的 SHA-256 一一对上、所有静态导入都落在包内、luxon 不留痕、`transparent` 与 440×620、页脚与「扫一次」不在。页面行为仍不做机械测试（不变量 4）。

- Python 3.9.25：全套 44 条 unittest 通过、零 skip（新增 8 条）；`compileall` 与 `git diff --check` 通过。新增的三条先在改之前的树上跑红，再转绿。
- 页面的三个模块另用 `node --check` 做过一次语法解析（临时目录里的副本，仓库不引入 node，也不引入 JS 测试跑器）。
- 改包之后记得按上面「页面与宿主」那条清 `%APPDATA%\zebar\webview-cache\loo0ng` 再开：这一轮换了 `index.html` 的骨架与 `style.css` 的整套变量，缓存没清会看到上一版的壳，包括上一版的报错页；清缓存会一并丢掉记在本地存储里的窗口位置。
- 材质、滚动条、窗口按钮、三种错误现身、默认尺寸下没有常驻滚动条这几项，归 Windows 人手验收（#20）在真宿主里逐项看，本票不代跑。
- 双轴审查（标准 / 规格）各跑一遍，据此改了五处：`window.js` 里那条守卫还写着旧类名 `.window-controls`（今天只是死代码，#17 把把手换成整卡之后会变成「点最小化＝拖窗」）；进度条不再靠 `[style="flex-grow: 0;"]` 这种内联样式字符串藏起 0 段，改成那一段干脆不画、空图留一条空槽；mac 的红绿灯恢复成悬停才现出 − 与 ×（原型里那句 `font-size: 0` 会把字形永久抹掉）；`backdrop-filter` 收进模拟档；`MATERIAL` 的 mac 那一行换掉了初版的 `hudWindow`（HUD 是深色材质，与浅色卡片和「不做深色模式」对不上）。没照做的两条记在这里：设置指引与横幅仍受全局 `user-select: none` 管，路径复制不出来——#12 的窗口裁定就是全局禁选，要开例外得先改那条；`扫描时间` 仍由扫描脚本出、已经没有消费方，等契约那一轮再收。

## #15 唤起脚本与 README 三入口

不变量 4 改了措辞：**测试缝是随包脚本的命令行**，现在有两条——`scan.py` 一个人做完发现、读、聚合与显示串格式化，`launch.py` 只找宿主、调一条命令。页面仍然只渲染，不计数、不排序、不取第一个、不算日期。两条缝都不引入 node，也不引入 JS 测试跑器。

`launch.py` 按 ADR-0001 办：PATH 里的 `zebar` 优先，没有就按平台挨个看固定路径（mac `/Applications/Zebar.app/Contents/MacOS/zebar`，Windows `C:\Program Files\glzr.io\Zebar\zebar.exe`），找到即以子进程调

```
<宿主> start-widget-preset --pack loo0ng --widget-name 案件卡片 --preset 默认
```

并透传子进程的退出码；都找不到就在 stdout 印一句「没找到 Zebar……」并退 1。脚本不做任何「带到前面」：宿主对已开着的预设无事发生，还原只能走任务栏 / 程序坞。

```powershell
python -m unittest discover -s tests -p 'test_launch.py'
python widget/launch.py
python widget/launch.py --平台 darwin --固定路径 'darwin=/tmp/zebar'
```

**测法**：`tests/test_launch.py` 只走命令行，起子进程跑 `launch.py`，不导入它的函数。假宿主是临时目录里一个把收到的参数写成 JSON、再按给定码退出的可执行文件（POSIX 上是 `zebar` 这个 `#!/bin/sh` 脚本，Windows 上是 `zebar.bat`，两边都转调本次的 `sys.executable`）。两个测试注入口：

- PATH 整个换成测试自己的目录，所以「PATH 里有 / 没有」两种情形都由测试说了算。
- `--固定路径 平台=路径` 可重复，给了就整张固定路径表都换成注入的，再按 `--平台` 选那一列。这样「按平台、按顺序找」有测试守，而两个真实安装路径的字面值不进测试——**测试一次都不许碰本机真装的 Zebar**，否则会在跑测试时把卡片开出来。那两个字面值由 Windows 人手验收（#20）和 mac 交付票守。

八条行为测试覆盖：参数正好是那一条命令、PATH 优先于固定路径、固定路径按顺序走且跳过不存在的、另一个平台那列碰都不碰、目录不会被当成宿主、都找不到时退 1 且那句话含安装去处、没有固定路径表的平台只看 PATH、假宿主的退出码透传。第九条是例外：它不跑脚本，只在 `launch.py` 的源码文本里找那两个真实安装路径的字面值。那两行没有别的守门——mac 那条连 #20 都盖不到——而一跑就会把本机真装的 Zebar 起来，所以只能盯字面值防手滑，真的跑通仍归人手验收。读包源码来断言在本仓库有先例（`tests/test_scan.py` 的禁用名那条）。

**白名单**：`launch.py` 不需要被资源服务器服务（它不是页面请求的资源），但放在包目录里随 README 那一条安装命令走。现有 `includeFiles` 的 `*.py` 本来就覆盖它，不用改 `zpack.json`；`privileges.shellCommands` 也不用动——那是页面调扫描脚本的口子，唤起脚本从卡片外面跑。

**Zebar 已在跑与没在跑，命令的返回不一样**：Zebar 3.3.1 的第二个实例把参数发给原实例后立刻退出（`setup_single_instance`），所以卡片已开时命令马上回来；Zebar 没在跑时，这个进程自己变成宿主，要等 Zebar 退出才返回。透传退出码是 #15 的验收项，所以这里不改成后台起进程，代价写在 README 里：Windows 的快捷方式只是多一个后台 `pythonw` 陪着宿主；**mac 的 `.command` 会让那扇终端窗口一直开着，关掉它会把 Zebar 一起关掉**。没拿 `nohup ... &` 把它甩开，是因为 mac 上一行都没实机跑过，不想交一个没验过的花招；这一条进 mac 交付票。

**那句「没找到 Zebar」在快捷方式上差点没人看见**：Windows 的快捷方式指 `pythonw.exe`（同一个 `python` 旁边那个，没有才退回 `python`），图的是不闪控制台窗口，但 `pythonw` 下 `sys.stdout` 是 `None`，照直 `print` 会崩，加个判空则是静悄悄退 1、律师什么都看不到。所以 `say()` 在没有 stdout 时于 Windows 上弹一个提示框。测试里 stdout 永远是管道，走不到弹框那一支，不会把测试挂在一个模态框上。

- Python 3.9.25：全套 36 条 unittest 通过、零 skip（新增 9 条）；`compileall` 与 `git diff --check` 通过；仓库无独立类型检查器。八条行为测试先在没有 `launch.py` 的树上跑红，再转绿。
- 源码禁用名那条测试自动覆盖 `launch.py`（它按后缀遍历整个包），继续通过。
- README 的 Windows 快捷方式命令已在本机跑过，只是把 `.lnk` 写进临时目录而不是桌面：目标、参数、工作目录三项回读正确。mac 那条只做了 `sh` 语法检查，没有实机跑过。
- 人手验收（关掉卡片后跑脚本、再跑一次、Zebar 退出后跑）归 #20，本票不代跑：跑一次就会在律师桌面上开窗。

## #21 当前模块改成当前节点所在的模块

`当前模块` 原先是「图序第一个进行中的模块」，`当前节点` 是「图序第一个已生成的节点（没有就退到下一个）」，两个字段各扫各的。第一个进行中的模块里一个已生成都没有时——律师跳着走，或者靠前有个空模块（空模块恒为进行中）——清单页第三层就印出「甲模块 › 乙节点」，而乙节点在乙模块里；`›` 在本仓库的读法（悬浮概览同形）是包含关系，那一行是在说一件不成立的事。

**新定义：`当前模块` 是当前节点所在的那个模块；当前节点为 `null` 时它也为 `null`。** 这一条取代 #12 实现裁定与 #14 AC 里「`当前模块` … 照旧」那句。只改扫描脚本：`first_in_state()` 按状态在图序里找第一个节点，连它所在的模块一起给出来，当前节点与当前模块从同一次查找里出来，不可能再对不上；`下一个` 仍是第一个未生成的节点，与当前节点的后备同源。输出不加字段，`当前节点` 仍是 `{标题,状态,高亮}` 三键，页面一个字没改——它本来就照印这两个字段，改完即名副其实。

丢掉的是「第一个进行中的模块」那个值：没有消费方用它（案件页表头按 #18 不放当前模块行）。

- 三条新测试先在旧实现上跑红再转绿，覆盖四档：错配那一档（后一个模块里才有已生成）、靠前空模块夺名那一档、一个已生成都没有退到下一个那一档、整案办完只剩一个空模块那一档（旧实现会印出那个空模块的名字，新实现两个字段一起为空）。
- Python 3.9.25：全套 27 条通过、零 skip；`compileall` 与 `git diff --check` 通过。四档排序不受影响，它看 `待看数` 与 `下一个`，不看 `当前模块`。
- 宿主验收（Windows Zebar 3.3.1，2026-09-22）：五个种子回放的工作区里没有这种形，所以用官方图引擎 CLI 在「在办中」的副本上 `add-module --title 空模块 --first` 造了第六个案「空模块靠前」，引擎自己重算视图。真窗口里那一行印的是「接受指定与报备 › 管理人印章备案报告」，靠前那个空模块没有夺走模块名；同一份合成根跑新旧两版扫描脚本对照，只有这一案变，其余五案逐字不变。
- 「跳着走」那种案（一个已生成都没有、退到下一个）新旧取值相同，宿主里的样子在 #14 那轮已经看过。
- 踩到一次：`compileall -q widget tests` 会在 `widget/` 下生成 `__pycache__`，跟着 `Copy-Item` 进了组件包（包内文件数 33 变 34）。装包前先清掉，开发说明「页面与宿主」一节早有这条，这次是自己犯的。
- 验后旧包装回、卡片设置按字节哈希恢复、宿主按原启动项重启，卡片保持验收前的关闭状态。

## #14 扫描契约扩展与清单页三层行

按 #12 的实现裁定扩了扫描输出，每案多两样：

- `模块[]`：每模块带 `标题`、`状态`、`进度`（未生成 / 已生成 / 已确认 / 不适用 / 总数）、`节点[]`；空模块留在数组里，只是节点为空。
- `节点`：`标题`、`状态`、`高亮`，引擎写了时限就带 `时限`；已确认带确认时刻、已生成带生成时刻，未生成与不适用两档一个时间字段都不带。时间给两份：`时间` 是引擎那串 ISO 原文，`时间显示` 是预先格式化好的「9月21日」，跨年才带年（「2019年1月5日」）。跨年按扫描那一刻的年份判，页面照印，不算日期。
- `当前节点{标题,状态,高亮}`：图序第一个已生成的节点；一个没有时退到下一个，都没有为 `null`。

`下一个` 与当前节点的后备都在图序里取第一个未生成的节点，不再读视图的 `前方`：引擎的前方装的正是还没生成的那些节点、按同一个序，两处同源就不会各说各话；从模块里取还多带状态与高亮两列，圆圈照它画。

去掉的：每行的前方列表、`待看` 名单、任何生成出来的路径。`当前模块`、`下一个`、`待看数`、`进度` 与顶层各键照旧。四档排序照旧，只是档位改看 `待看数` 与 `下一个`（`下一个` 非空恰好就是有前方）。

`已生成` 与 `已确认` 的节点必须带得出时刻，带不出按结构不完整算，整案落到 `读不出`，不猜、不降级，和格式版本那一档一个路子。

三条守门测试盯着输出：不含 `"条目"`、不含 `"文书"` 与 `"审查报告"` 键及任何文书路径、不含 `"前方"` 键；「源码只出现一个工作区文件名」那条继续过。

本票交付时 `当前模块` 仍是「图序第一个进行中的模块」，与 `当前节点` 各算各的，第三层因此会印出名不副实的一行。那处错配由 #21 治掉，新定义见下一节。

页面只渲染：分段进度条把四个计数直接交给 `flex-grow`，不算比例；圆圈按扫描给的状态与高亮挑类名，未生成空心、已生成半实、已确认实心、不适用淡灰带斜线，高亮未清加一圈琥珀。行没有点击行为（案件页是 #18）。外壳、页脚、窗口这票不动，归 #16。

- Python 3.9.25：24 条 unittest 全部通过、零 skip；`compileall` 与 `git diff --check` 通过；仓库无独立类型检查器。新增与改写的测试先在旧实现上跑红（当前节点、按模块计数、时间与显示串、时限、三条守门），再转绿。
- 端到端：用 loo0ng-skills 的 `scripts/skill-eval.py --materialize` 回放三个种子（在办中、两份待确认、空图）的工作区，凑成一个合成根跑 CLI：三案读出、模块与节点齐、时间只在该有的两档出现、显示串是「M月D日」，四样禁用键一个都没出现。合成数据只在临时目录里，不进仓库。
- 页面文件改了，装进宿主看之前要按[页面与宿主](#页面与宿主)清该包的 WebView 缓存，否则看到的还是上一版的行。
- 2026-09-22，Windows Zebar 3.3.1 宿主验收通过。合成现场是五个种子回放出来的工作区（在办中、两份待确认、跳着走、整块不走、空图），放在仓库被忽略的 `.scratch/` 下，不进包。按本地开发装法装包、清该包的 WebView 缓存、把卡片设置临时指过去，再用 ADR-0001 那条 `start-widget-preset` 命令开卡片（顺带证明那条命令行可用）。
- 默认预设 460 × 420 CSS 像素（本机 150% 缩放下物理 690 × 630，窗口在 (1906, 36)）下五行如预期，四档排序在真窗口里成立：两份待确认、在办中（两案有待看）、跳着走、整块不走（两案有前方）、空图垫底。
- 三种圆圈各验到：**半实**（两份待确认，当前节点已生成、高亮已清）、**半实加一圈琥珀**（在办中，已生成且高亮未清）、**空心**（跳着走与整块不走，一个已生成的都没有、退到下一个）。空图那行没有圈，第三层只有一个破折号。
- 分段进度条按计数画：整块不走是「已确认 1 / 已生成 0 / 未生成 69 / 不适用 2」，放大看是左端一小截深绿、中间一长条浅绿、右端一小截灰，已生成那段宽度为零不占位。
- 行点击暂无事：点一行之后整窗截图与点击前逐字节一致，窗口矩形也没变（点行不会误拖窗）。
- 轮询不打断画面：滚到下半屏后经过多轮五秒刷新，滚动位置与内容都不跳。
- 顺带记下：旧的安装里留着一份历史遗留的 `__pycache__\scan.cpython-39.pyc`，这次装的包没有（33 个文件）。清 WebView 缓存会把记在本地存储里的窗口位置一并清掉，卡片回到预设位置，这是预期。
- 验后按字节哈希恢复卡片设置（前后一致），宿主按原启动项重启；`~/.glzr/zebar/settings.json` 全程未动，没有用临时 `startupConfig`。新版组件包保留，验收前的旧包与设置留在 `~/loo0ng-验收备份`。截图只截卡片窗口那块矩形，不截整屏，也不进仓库。

## #10 老格式的读不出文案

`格式版本` 恰为 1 时，`aggregate()` 抛的不再是通用那句，而是一句律师照着能做的话：

```
老格式（格式版本 1）：这一案工作台不接，照原来的办法办，别对它打起手
```

**没有写成「从预设图重新起手」**，尽管引擎对同一件事的原话是那样。原因在消费方这一侧：这种案子多半已经办了一半，而起手会把工作区根上原有的一切挪进别处，所以那句照搬过来会教人做一件有害的事。0、3 及以后的版本维持原状：照实报版本号与“只认 2”，那时候该做什么这张卡片答不了。

- Python 3.9.25：全套 18 条 unittest 通过（新增一条守这一档），`compileall` 与 `git diff --check` 通过；仓库无独立类型检查器。
- 新测试先验证旧实现失败（断言落在“别对它打起手”那一句上），再转绿；同时断言这一档不含“只认 2”，与版本 3 那条区分开。
- 包源码禁用名称那条边界测试继续通过：新文案不含任何被禁的工作区目录名或文件名。
- 2026-09-22，Windows Zebar 3.3.1 宿主验收通过。合成现场三个工作区：甲（格式版本 1）、乙（格式版本 2，正常）、丙（格式版本 3），只写在忽略目录里，不进包。临时把卡片设置的根目录指过去，验后按字节哈希恢复原设置。
- 默认预设 460 × 420 CSS 像素（本机 150% 缩放下物理 690 × 630）下，甲那一句**折成两行完整显示，没有截断、没有省略号**；丙那句「格式版本是 3，这张卡片只认 2」在它上面单独一行，两档一眼分得开，乙一行正常。`.unreadable` 只有 `overflow-wrap: anywhere`，不设 `nowrap` 或省略号，所以变窄只会继续折行。
- 启动用的是临时 `startupConfig`，本地包的 `pack` 就写 `loo0ng`；写成 `local.loo0ng` 会在 `~/.glzr/zebar/errors.log` 报 `No widget pack found`。验后 `settings.json` 按字节哈希恢复。
- 本轮只换了 `scan.py`，页面资源没动，因此没有清 WebView 缓存，记在本地存储里的窗口位置保留。截图期间从外部挪过窗口，`remember()` 只在卡片自己拖动成功后写，位置未被覆盖。
- 复跑 Python 3.9.25：18 条 unittest 通过，`compileall` 通过。
- 顺带看到：默认尺寸下页面右侧有一条竖直滚动条，内容并未溢出（页脚可见）。不在 #10 范围内，本轮不动。

## #7 窗口行为

`window.js` 使用随包客户端的当前组件和 Tauri 窗口接口；启动时加载模块并恢复位置后启用控件。按住表头用同步的 `pointerdown` 开始拖动（对应鼠标按下，增加指针捕获以免快速拖出窗口时丢失事件），按屏幕 CSS 位移乘 `devicePixelRatio` 转为物理位置。`requestAnimationFrame` 合并待移动位置，宿主调用逐次完成，松手、失焦或指针取消时结束拖动；最小化、关闭前处理完最后一次移动。

Windows 控件在右上角，关闭悬停为红色；mac 用同一段 HTML 在左侧显示关闭、最小化两个圆钮，关闭排最左。关闭调用当前组件的 `close()`；最小化调用当前窗口的 `minimize()`，从任务栏或程序坞恢复。配置保留 `focused: true`、`shownInTaskbar: true`、`zOrder: normal` 和唯一的「默认」预设。

唯一持久化内容是 WebView `localStorage` 中的 `loo0ng.window-position` 坐标；只在卡片主动移动成功后保存，系统最小化报告的位置不覆盖它。缺失、无效或不可读的存储使用预设位置；存储不可写不影响本次拖动。清 WebView 缓存后回到预设。窗口错误单独显示，不被扫描刷新清除。

- 2026-09-17：Python 3.9.25 的 17 条 unittest、编译检查、依赖哈希与导入闭包检查通过，未新增测试缝或类型检查器。`window.js` 受已有 `*.js` 白名单覆盖，客户端依赖没有新增。
- 双轴审查：Standards 无发现；Spec 无确定实现缺陷。审查提示系统最小化可能报告离屏坐标，已将保存限制为主动移动成功后。
- 已安装到 Windows Zebar 3.3.1，以独立 WebView 缓存和既有三个种子回放工作区启动；实际窗口显示三个合成案件、窗口按钮已启用、没有初始化错误。原设置和组件已备份，合成验收现场暂时保留。
- 尚待人手验收：拖动（含非 100% 缩放）、失焦后首次点击、最小化并从任务栏恢复、关闭只影响当前组件、重开恢复位置、清缓存退回预设。mac 实机验收不在首版范围内。本票暂不关闭。
- 2026-09-17，用户确认「回到位置了、跟手、最小化能恢复」：拖动、最小化后任务栏恢复、关闭后重新打开的位置记忆已通过。尚未确认非 100% 缩放、失焦后首次点击、其他组件不受关闭影响与清缓存退回预设，不能据此关闭整票。
- 托盘入口说明：Zebar 3.3.1 在 Windows 上左键点击托盘图标会打开设置页；卡片关闭后应右键托盘 → `Widget packs` → `loo0ng` → `案件卡片` → `Run '默认'`。该菜单项是启动/停止开关，已勾选时点击会关闭组件，不是还原最小化窗口。最小化应从任务栏恢复。依据 [Zebar 3.3.1 sys_tray.rs](https://github.com/glzr-io/zebar/blob/v3.3.1/packages/desktop/src/sys_tray.rs)。此前「从托盘重新打开」的验收指引缺少右键菜单路径，现予补正。
- 2026-09-17，用户进一步确认此前三项人手操作均通过（拖动；失焦后点击最小化并从任务栏恢复；关闭不影响宿主和其他组件、重开回到保存位置）。当前宿主实测 `devicePixelRatio = 1.5`，覆盖非 100% 缩放的拖动验收。
- 补验清缓存：退出宿主后将专用验收缓存移至备份，以相同缓存路径空目录启动；实际 `localStorage` 位置键为 `null`，窗口物理位置为 `(1906, 36)`，尺寸 `690 × 630`，对应本机 150% 缩放下的预设位置与尺寸。控件正常启用、无窗口初始化错误。随后恢复已有位置缓存，原设置按字节哈希核对恢复，撤下临时诊断端口；新版组件保留。
- #7 的 Windows 验收完成。用户明确提出拖拽区域需要重新设计，并决定在后续 UI 设计和功能设计阶段处理；该设计项保留为后续工作，不在本轮调整拖拽区域。

## #6 安装与说明验证记录

- 2026-09-17，Windows PowerShell 5.1、Zebar 3.3.1：把原组件移到宿主目录外备份，在不存在的 `loo0ng` 目标上原样执行 README 中的 Windows 命令。无需 GitHub 登录，下载并安装成功；32 个文件与公开归档逐一核对 SHA-256 一致，包含整个 vendor。再次执行会拒绝覆盖已有组件。
- 使用独立 WebView 缓存启动，宿主日志确认包有效并创建「案件卡片」窗口，进程窗口标题为 `Zebar - loo0ng / 案件卡片`。验后恢复原组件和正常启动；设置文件的前后哈希一致。
- 命令下载的是 GitHub 已发布的 `main`。本次远端页面代码落后于本地主线，所以此次验证证明下载安装与宿主建窗成功，不代表本地最新功能已通过这条公开命令交付。开发者需先将实现发布到远端，用户安装才会取得对应版本。
- 本地包和下载包的 31 个资源均被现有 `includeFiles` 覆盖（另有宿主读取的 `zpack.json`），所有包内文件名均为 ASCII；白名单无需改动。
- README 的 mac 命令通过 Bash 语法检查，未在 mac 执行，符合本票不做 mac 验收的范围。开发说明的 PowerShell 预览导出命令使用已有种子回放根验证，生成可解析的 UTF-8 JSON。
- Python 3.9.25：17 条 unittest 全部通过，编译检查和 `git diff --check` 通过；本仓库没有独立类型检查器。本票不改扫描行为，不增加测试缝。
- 2026-09-17，用户确认已按 README 的 Windows 命令完成安装，并看到卡片正常显示；母票要求的人手安装验收已完成。mac 命令仍未实机验收，不在本票验收范围内。
- 复核：Python 3.9.25 的 17 条 unittest、编译检查以及本地包和下载包的 31 个资源白名单检查全部通过。
- 双轴审查：Standards 无发现；Spec 无实现缺陷。人手验收缺口已由上述用户确认闭合，#6 验收完成。

## #5 四档排序与展开验收记录

- Python 3.9.25：17 条 unittest 全部通过，编译检查通过。排序测试先验证旧实现失败，再验证四档顺序、每档中文名升序，以及改变生成时间后连续两轮顺序一致；已有测试继续覆盖跨模块前五个节点与原样时限。
- 合成验收根已在本机准备：复用在办中、空图种子回放视图，另回放图引擎种子；完成档通过官方图 CLI 将副本的全部模块标为不适用生成。CLI 输出顺序与四档一致，临时数据不提交。
- 双轴审查：Standards 无发现；Spec 无明确实现偏差。mac 的中文 locale 可用性与实际顺序需在后续交付时确认。
- 2026-09-17 用户授权安装、临时切换合成根目录及重启 Zebar。安装后的扫描脚本、页面脚本、样式与仓库文件哈希一致；真窗口读出四案，顺序为乙（待看）、甲（有前方）、丙（空图）、丁（没有前方），后两档提示不同，扫描时刻持续更新。
- 用户现场确认：「顺序、展开收起和单案切换都正确。」据此闭合真窗口人手验收缺口，#5 的 Windows 验收完成。
- 验后按字节哈希核对恢复原设置，正常重启宿主；保留新版组件包和本机备份，合成数据不提交。原设置指向不存在的根目录，恢复后的相应提示是预期状态。

## #4 错误隔离与设置指引

扫描输出新增 `设置错误`：正常为 `null`，失败为 `{"类别": "…", "原因": "…"}`。
四种类别是 `文件不存在`、`不是 JSON`、`缺少根目录`、`根目录为空`；另对不可读设置和无效路径数组给出明确错误。
原因含实际设置路径、可填写的 JSON 形状及 Windows 路径提示。设置失败仍返回成功的 JSON 输出，案件数为零，页面显示指引并继续每五秒重试；修好后指引消失。`--根` 继续绕过设置。

坏 JSON、无法读取的视图、不支持的版本以及不存在的根目录均隔离到 `读不出`，页面置于正常案件下面。版本原因保留实际版本；版本恰为 1 的那一档另有一句可照做的话，见 #10。

- Python 3.9.25：全套 15 条 unittest 通过，编译检查和 `git diff --check` 通过；仓库无独立类型检查器。
- 无法读取的用例在 Windows 使用真实文件独占句柄，CLI 在独立进程中读文件；非 root 的 POSIX 使用文件权限，不模拟扫描内部函数。
- Windows Zebar 3.3.1 自动宿主验证：使用此前种子回放视图的临时副本，将其中一份改成坏 JSON，确认它移到最底下，其余两行不变。
- 四种设置错误逐一在宿主显示路径和示例；恢复有效设置后，三行恢复且提示消失。截图已检查，无文本截断。验后恢复原设置与正常宿主启动，保留更新的组件包。
- 2026-09-17 用户确认两项人手验收：坏视图场景中两案正常，坏案在下方显示读不出；移除设置后，设置文件路径和 JSON 填写示例均显示。#4 验收完成。
- 双轴审查：Standards 无发现；Spec 无代码缺陷，人手验收缺口已由上述用户确认闭合。
- 现场反馈曾出现“只有读不出、另两案不显示”。核查恢复后的原设置：所指根目录不存在，CLI 返回零案和根目录错误。重新固定合成根目录后 CLI 返回两案与一个坏 JSON 错误；后续人手验收逐步确认后再切场景，避免自动切换与恢复设置混淆观察。
- 人手验收结束后，按字节核对恢复原设置，还原临时合成视图；保留更新的组件包。原设置指向不存在的根目录，恢复后的相应提示是预期状态。

## #2 验收记录

- Python 3.9.25：全套 11 条 unittest 通过；`compileall` 和 `git diff --check` 通过。本仓无独立类型检查器。
- 端到端合成来源：在办中、重出、空图三个种子，通过 loo0ng-skills 的 `scripts/skill-eval.py --materialize` 回放。输出只用于临时验收，不提交到版本控制。
- 已成功安装到 Zebar 3.3.1，宿主日志确认组件包有效并创建案件卡片窗口。
- 用户已确认真实窗口显示三个案件，初始进度为 `1/72`、`1/72`、`0/0`，扫描时刻每五秒更新。
- 三案扫描字段已与律师读的派生视图核对：节点数、四种状态计数、当前模块、待看和下一个一致。
- 官方图 CLI 已在合成 case-b 追加确认，扫描进度变为 `2/72`。2026-09-17 用户确认卡片下一轮（约五秒内）自动更新，另两案保持 `1/72` 和 `0/0`。此项以用户现场验收为依据。
- 双轴代码审查：Standards 无发现；Spec 无代码缺陷，宿主更新验收缺口已闭合。
- 验收结束后恢复原卡片设置，保留已安装的实现包。

#2 验收完成；首版其余执行票仍按各自范围验收。

## #3 离线依赖验收记录

- 使用首选的随包方案，没有远程 import 退路，不需要改动另一个仓库的 ADR。
- 2026-09-17，Windows Zebar 3.3.1：退出宿主，以全新 WebView 用户数据目录冷启动。
  WebView 设置不可用的外部代理 `http://127.0.0.1:9`，仅 localhost、127.0.0.1、[::1] 绕过代理，
  以此阻断外部网络、保留宿主本机通信；没有断开整台机器的网络。
- 使用 #2 留存的三个种子回放工作区；真实宿主 DOM 与扫描 CLI 输出核对，案件数为 3，
  进度为 `1/72`、`2/72`、`0/0`，当前模块、下一个与待看数全部一致，页面没有报错。
- 通过本机 CDP 读取冷启动资源记录（37 条）；开启网络记录、禁用并清空浏览器缓存再重载，
  观察 12 秒（覆盖两轮轮询），再手动扫描。重载后记录 28 条请求，没有外部地址请求、没有加载失败，
  三行保持一致，扫描时刻自动更新，手动按钮也正常。白名单中的 16 个客户端模块实际加载成功。
- 本次不增加新的单测缝：扫描逻辑未改，离线与资源加载在真实宿主上验收；Python 3.9.25 的
  全套 11 条 unittest、编译检查、依赖闭包与哈希核对、`git diff --check` 通过。
- 验收后恢复原卡片设置和正常宿主启动，保留已安装的本地依赖包。临时截图、网络记录与合成数据不提交。
- 双轴审查：Standards 无发现；Spec 保留一项验收缺口——上述代理隔离验证不等于整机断网。
  #3 的「拔网线（或断开网络）重启宿主」仍待现场验证，因此尚不能将该票标为全部验收完成。
- 2026-09-17，补做整机断网冷启动：备份原设置后切至上述三个合成案件，使用每次新建的 WebView 缓存启动脚本，不设置代理。用户按断网、重启、核对三案内容与刷新、恢复网络的步骤验收后确认「显示正确」。由此闭合此前的整机断网验收缺口，#3 验收完成。
- 本次复核 Python 3.9.25 的 17 条 unittest、编译检查，以及 16 个随包模块的 SHA-256、相对导入闭包、白名单与 ASCII 文件名，全部通过。实现未改，沿用此前双轴审查；验收后恢复原设置并按字节哈希核对。

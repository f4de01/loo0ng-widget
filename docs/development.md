# 开发与验证

实现规格是 [#2](https://github.com/f4de01/loo0ng-widget/issues/2)，母票是 [#1](https://github.com/f4de01/loo0ng-widget/issues/1)。原型分支只作资料，不合并。

Windows 首版已通过 #8 最终验收，逐项证据、机器与版本、Python 3.9 跑法及现场恢复情况见 [Windows 验收记录](acceptance-windows.md)。下文保留各实现票当时的验证过程与后续补验。

## 包与扫描

`widget/` 是组件包。`scan.py` 是唯一测试缝，完成一层发现、读取和聚合。只支持 Python 3.9 及以上和标准库，没有 Node、JS 测试跑器或类型检查器。

```powershell
python -m unittest discover -s tests -p 'test_*.py'
python -m compileall -q widget tests
python widget/scan.py --根 <合成案件根目录>
python widget/scan.py --设置 <设置文件>
```

`--根` 可重复，提供它就不读取设置。默认设置是 `~/.loo0ng/卡片设置.json`，形状为 `{"根目录":["绝对路径"]}`。

输出沿用 #2 的 `扫描时间`、`设置文件`、`根目录`、`行`、`读不出`；另加顶层 `案件数`（成功读出的案件数）和每行 `待看数`。两个计数由扫描脚本生成，页面不再计算数组长度。用户已确认这一契约补充。

#5 的案件排序由脚本完成：有待看、有前方、空图、没有前方四档；同档按系统 `zh_CN.UTF-8` 中文排序，目录名相同时再按路径固定顺序，不按生成时间。没有中文 locale 时明确报错，不静默退回 Unicode 序。Windows 的 Python 3.9.25 已验证；mac 的系统排序仍待交付验收。

页面按母票的提示文案区分空图与没有前方，只检查前方是否为空，不计算数量。点击一行展开前方头五个节点，时限原样显示；同时只展开一案，按工作区路径在轮询刷新后保持展开，案件消失则清除展开状态。

合成视图手写在 `tests/test_scan.py`，测试不调用引擎。两条边界测试分别检查包源码禁用名称和输出不含条目键。检查禁用名称前移除允许的完整文件名，避免把其中的后缀误认成另一个文件名。

## 页面与宿主

发布版按 [README](../README.md#装法) 的一条命令安装；本地开发则把 `widget/` 的内容装到 `~/.glzr/zebar/loo0ng/`，通过 Zebar 的「案件卡片 / 默认」打开。已有安装先备份到 Zebar 目录外。

`zpack.json` 的 `includeFiles` 是资源服务器的白名单，不是附带清单。`htmlPath` 指向的文件未被覆盖时，宿主直接回 500。新增资源必须同时加入白名单：当前 `*.html`、`*.css`、`*.js`、`*.py` 覆盖包入口，`vendor/**` 覆盖完整依赖、manifest、说明和许可证。`zpack.json` 是宿主读取的包描述，不是页面请求的资源；安装时仍必须复制它。不要把本机生成的 `__pycache__/` 拷进包里。

页面直接按扫描数组顺序渲染。每五秒发起扫描，失败显示错误并保留上一轮内容和时刻；下一轮继续。手动按钮与轮询共享忙碌状态，避免重叠执行。

未识别宿主时直接报错。不装宿主调样式时，在仓库根目录运行 `python -m http.server 8000 --bind 127.0.0.1 --directory widget`，浏览器打开 `http://127.0.0.1:8000/index.html?dev=1`，在页面里手动选择扫描 CLI 导出的 UTF-8 JSON。用完在终端按 Ctrl+C 停止服务器。这条显式通道不轮询，也不替代宿主验收；不带 `?dev=1` 会明确报未连接宿主。

只用合成案件生成预览 JSON。PowerShell 5.1 的 `>` 默认会写成 UTF-16，可在合成根已准备好后用下面的方式保存（将路径换成你的合成根）：

```powershell
$env:PYTHONIOENCODING = 'utf-8'
[Console]::OutputEncoding = [Text.UTF8Encoding]::new($false)
$preview = python widget/scan.py --根 'D:/合成案件'
if ($LASTEXITCODE -ne 0) { throw '扫描失败' }
New-Item -ItemType Directory -Force .scratch | Out-Null
[IO.File]::WriteAllText((Join-Path $PWD '.scratch/preview.json'), ($preview -join "`n"), [Text.UTF8Encoding]::new($false))
```

开发时每次改包后，先关闭该组件（缓存被占用就退出 Zebar），确认 `%APPDATA%\zebar\webview-cache\loo0ng` 是该包的缓存目录，再清理并重新打开。否则看到的可能还是上一版，包括上一版的报错页。只清 `loo0ng`，勿清其他组件缓存；以后记在 WebView 本地存储里的窗口位置也会随清缓存丢失。

客户端固定为 `zebar@3.3.1`，通过 `vendor/zebar-3.3.1.js` 本地导入；完整浏览器依赖、来源哈希和许可证均随包提供，`vendor/**` 纳入资源白名单。升级步骤见 [随包客户端](../widget/vendor/README.md)。一条命令安装属于 #6，窗口控件与拖动属于 #7，最终人手验收属于 #8。

## #10 老格式的读不出文案

`格式版本` 恰为 1 时，`aggregate()` 抛的不再是通用那句，而是一句律师照着能做的话：

```
老格式（格式版本 1）：这一案工作台不接，照原来的办法办，别对它打起手
```

**没有写成「从预设图重新起手」**，尽管引擎对同一件事的原话是那样。原因在消费方这一侧：这种案子多半已经办了一半，而起手会把工作区根上原有的一切挪进别处，所以那句照搬过来会教人做一件有害的事。0、3 及以后的版本维持原状：照实报版本号与“只认 2”，那时候该做什么这张卡片答不了。

- Python 3.9.25：全套 18 条 unittest 通过（新增一条守这一档），`compileall` 与 `git diff --check` 通过；仓库无独立类型检查器。
- 新测试先验证旧实现失败（断言落在“别对它打起手”那一句上），再转绿；同时断言这一档不含“只认 2”，与版本 3 那条区分开。
- 包源码禁用名称那条边界测试继续通过：新文案不含任何被禁的工作区目录名或文件名。
- 2026-09-22，Windows Zebar 3.3.1 宿主验收通过。合成现场三个工作区：甲（格式版本 1）、乙（格式版本 2，正常）、丙（格式版本 3），只写在忽略目录里，不进包。临时把卡片设置的根目录指过去，验后按字节哈希恢复原设置。
- 默认预设 460 × 420 CSS 像素（本机 150% 缩放下物理 690 × 630）下，甲那一句**折成两行完整显示，没有截断、没有省略号**；丙那句「格式版本是 3，这张卡片只认 2」在它上面单独一行，两档一眼分得开，乙一行正常。`.unreadable` 只有 `overflow-wrap: anywhere`，不设 `nowrap` 或省略号，所以变窄只会继续折行。
- 启动用的是临时 `startupConfig`，本地包的 `pack` 就写 `loo0ng`；写成 `local.loo0ng` 会在 `~/.glzr/zebar/errors.log` 报 `No widget pack found`。验后 `settings.json` 按字节哈希恢复。
- 本轮只换了 `scan.py`，页面资源没动，因此没有清 WebView 缓存，记在本地存储里的窗口位置保留。截图期间从外部挪过窗口，`remember()` 只在卡片自己拖动成功后写，位置未被覆盖。
- 复跑 Python 3.9.25：18 条 unittest 通过，`compileall` 通过。
- 顺带看到：默认尺寸下页面右侧有一条竖直滚动条，内容并未溢出（页脚可见）。不在 #10 范围内，本轮不动。

## #7 窗口行为

`window.js` 使用随包客户端的当前组件和 Tauri 窗口接口；启动时加载模块并恢复位置后启用控件。按住表头用同步的 `pointerdown` 开始拖动（对应鼠标按下，增加指针捕获以免快速拖出窗口时丢失事件），按屏幕 CSS 位移乘 `devicePixelRatio` 转为物理位置。`requestAnimationFrame` 合并待移动位置，宿主调用逐次完成，松手、失焦或指针取消时结束拖动；最小化、关闭前处理完最后一次移动。

Windows 控件在右上角，关闭悬停为红色；mac 用同一段 HTML 在左侧显示关闭、最小化两个圆钮，关闭排最左。关闭调用当前组件的 `close()`；最小化调用当前窗口的 `minimize()`，从任务栏或程序坞恢复。配置保留 `focused: true`、`shownInTaskbar: true`、`zOrder: normal` 和唯一的「默认」预设。

唯一持久化内容是 WebView `localStorage` 中的 `loo0ng.window-position` 坐标；只在卡片主动移动成功后保存，系统最小化报告的位置不覆盖它。缺失、无效或不可读的存储使用预设位置；存储不可写不影响本次拖动。清 WebView 缓存后回到预设。窗口错误单独显示，不被扫描刷新清除。

- 2026-09-17：Python 3.9.25 的 17 条 unittest、编译检查、依赖哈希与导入闭包检查通过，未新增测试缝或类型检查器。`window.js` 受已有 `*.js` 白名单覆盖，客户端依赖没有新增。
- 双轴审查：Standards 无发现；Spec 无确定实现缺陷。审查提示系统最小化可能报告离屏坐标，已将保存限制为主动移动成功后。
- 已安装到 Windows Zebar 3.3.1，以独立 WebView 缓存和既有三个种子回放工作区启动；实际窗口显示三个合成案件、窗口按钮已启用、没有初始化错误。原设置和组件已备份，合成验收现场暂时保留。
- 尚待人手验收：拖动（含非 100% 缩放）、失焦后首次点击、最小化并从任务栏恢复、关闭只影响当前组件、重开恢复位置、清缓存退回预设。mac 实机验收不在首版范围内。本票暂不关闭。
- 2026-09-17，用户确认「回到位置了、跟手、最小化能恢复」：拖动、最小化后任务栏恢复、关闭后重新打开的位置记忆已通过。尚未确认非 100% 缩放、失焦后首次点击、其他组件不受关闭影响与清缓存退回预设，不能据此关闭整票。
- 托盘入口说明：Zebar 3.3.1 在 Windows 上左键点击托盘图标会打开设置页；卡片关闭后应右键托盘 → `Widget packs` → `loo0ng` → `案件卡片` → `Run '默认'`。该菜单项是启动/停止开关，已勾选时点击会关闭组件，不是还原最小化窗口。最小化应从任务栏恢复。依据 [Zebar 3.3.1 sys_tray.rs](https://github.com/glzr-io/zebar/blob/v3.3.1/packages/desktop/src/sys_tray.rs)。此前「从托盘重新打开」的验收指引缺少右键菜单路径，现予补正。
- 2026-09-17，用户进一步确认此前三项人手操作均通过（拖动；失焦后点击最小化并从任务栏恢复；关闭不影响宿主和其他组件、重开回到保存位置）。当前宿主实测 `devicePixelRatio = 1.5`，覆盖非 100% 缩放的拖动验收。
- 补验清缓存：退出宿主后将专用验收缓存移至备份，以相同缓存路径空目录启动；实际 `localStorage` 位置键为 `null`，窗口物理位置为 `(1906, 36)`，尺寸 `690 × 630`，对应本机 150% 缩放下的预设位置与尺寸。控件正常启用、无窗口初始化错误。随后恢复已有位置缓存，原设置按字节哈希核对恢复，撤下临时诊断端口；新版组件保留。
- #7 的 Windows 验收完成。用户明确提出拖拽区域需要重新设计，并决定在后续 UI 设计和功能设计阶段处理；该设计项保留为后续工作，不在本轮调整拖拽区域。

## #6 安装与说明验证记录

- 2026-09-17，Windows PowerShell 5.1、Zebar 3.3.1：把原组件移到宿主目录外备份，在不存在的 `loo0ng` 目标上原样执行 README 中的 Windows 命令。无需 GitHub 登录，下载并安装成功；32 个文件与公开归档逐一核对 SHA-256 一致，包含整个 vendor。再次执行会拒绝覆盖已有组件。
- 使用独立 WebView 缓存启动，宿主日志确认包有效并创建「案件卡片」窗口，进程窗口标题为 `Zebar - loo0ng / 案件卡片`。验后恢复原组件和正常启动；设置文件的前后哈希一致。
- 命令下载的是 GitHub 已发布的 `main`。本次远端页面代码落后于本地主线，所以此次验证证明下载安装与宿主建窗成功，不代表本地最新功能已通过这条公开命令交付。开发者需先将实现发布到远端，用户安装才会取得对应版本。
- 本地包和下载包的 31 个资源均被现有 `includeFiles` 覆盖（另有宿主读取的 `zpack.json`），所有包内文件名均为 ASCII；白名单无需改动。
- README 的 mac 命令通过 Bash 语法检查，未在 mac 执行，符合本票不做 mac 验收的范围。开发说明的 PowerShell 预览导出命令使用已有种子回放根验证，生成可解析的 UTF-8 JSON。
- Python 3.9.25：17 条 unittest 全部通过，编译检查和 `git diff --check` 通过；本仓库没有独立类型检查器。本票不改扫描行为，不增加测试缝。
- 2026-09-17，用户确认已按 README 的 Windows 命令完成安装，并看到卡片正常显示；母票要求的人手安装验收已完成。mac 命令仍未实机验收，不在本票验收范围内。
- 复核：Python 3.9.25 的 17 条 unittest、编译检查以及本地包和下载包的 31 个资源白名单检查全部通过。
- 双轴审查：Standards 无发现；Spec 无实现缺陷。人手验收缺口已由上述用户确认闭合，#6 验收完成。

## #5 四档排序与展开验收记录

- Python 3.9.25：17 条 unittest 全部通过，编译检查通过。排序测试先验证旧实现失败，再验证四档顺序、每档中文名升序，以及改变生成时间后连续两轮顺序一致；已有测试继续覆盖跨模块前五个节点与原样时限。
- 合成验收根已在本机准备：复用在办中、空图种子回放视图，另回放图引擎种子；完成档通过官方图 CLI 将副本的全部模块标为不适用生成。CLI 输出顺序与四档一致，临时数据不提交。
- 双轴审查：Standards 无发现；Spec 无明确实现偏差。mac 的中文 locale 可用性与实际顺序需在后续交付时确认。
- 2026-09-17 用户授权安装、临时切换合成根目录及重启 Zebar。安装后的扫描脚本、页面脚本、样式与仓库文件哈希一致；真窗口读出四案，顺序为乙（待看）、甲（有前方）、丙（空图）、丁（没有前方），后两档提示不同，扫描时刻持续更新。
- 用户现场确认：「顺序、展开收起和单案切换都正确。」据此闭合真窗口人手验收缺口，#5 的 Windows 验收完成。
- 验后按字节哈希核对恢复原设置，正常重启宿主；保留新版组件包和本机备份，合成数据不提交。原设置指向不存在的根目录，恢复后的相应提示是预期状态。

## #4 错误隔离与设置指引

扫描输出新增 `设置错误`：正常为 `null`，失败为 `{"类别": "…", "原因": "…"}`。
四种类别是 `文件不存在`、`不是 JSON`、`缺少根目录`、`根目录为空`；另对不可读设置和无效路径数组给出明确错误。
原因含实际设置路径、可填写的 JSON 形状及 Windows 路径提示。设置失败仍返回成功的 JSON 输出，案件数为零，页面显示指引并继续每五秒重试；修好后指引消失。`--根` 继续绕过设置。

坏 JSON、无法读取的视图、不支持的版本以及不存在的根目录均隔离到 `读不出`，页面置于正常案件下面。版本原因保留实际版本；版本恰为 1 的那一档另有一句可照做的话，见 #10。

- Python 3.9.25：全套 15 条 unittest 通过，编译检查和 `git diff --check` 通过；仓库无独立类型检查器。
- 无法读取的用例在 Windows 使用真实文件独占句柄，CLI 在独立进程中读文件；非 root 的 POSIX 使用文件权限，不模拟扫描内部函数。
- Windows Zebar 3.3.1 自动宿主验证：使用此前种子回放视图的临时副本，将其中一份改成坏 JSON，确认它移到最底下，其余两行不变。
- 四种设置错误逐一在宿主显示路径和示例；恢复有效设置后，三行恢复且提示消失。截图已检查，无文本截断。验后恢复原设置与正常宿主启动，保留更新的组件包。
- 2026-09-17 用户确认两项人手验收：坏视图场景中两案正常，坏案在下方显示读不出；移除设置后，设置文件路径和 JSON 填写示例均显示。#4 验收完成。
- 双轴审查：Standards 无发现；Spec 无代码缺陷，人手验收缺口已由上述用户确认闭合。
- 现场反馈曾出现“只有读不出、另两案不显示”。核查恢复后的原设置：所指根目录不存在，CLI 返回零案和根目录错误。重新固定合成根目录后 CLI 返回两案与一个坏 JSON 错误；后续人手验收逐步确认后再切场景，避免自动切换与恢复设置混淆观察。
- 人手验收结束后，按字节核对恢复原设置，还原临时合成视图；保留更新的组件包。原设置指向不存在的根目录，恢复后的相应提示是预期状态。

## #2 验收记录

- Python 3.9.25：全套 11 条 unittest 通过；`compileall` 和 `git diff --check` 通过。本仓无独立类型检查器。
- 端到端合成来源：在办中、重出、空图三个种子，通过 loo0ng-skills 的 `scripts/skill-eval.py --materialize` 回放。输出只用于临时验收，不提交到版本控制。
- 已成功安装到 Zebar 3.3.1，宿主日志确认组件包有效并创建案件卡片窗口。
- 用户已确认真实窗口显示三个案件，初始进度为 `1/72`、`1/72`、`0/0`，扫描时刻每五秒更新。
- 三案扫描字段已与律师读的派生视图核对：节点数、四种状态计数、当前模块、待看和下一个一致。
- 官方图 CLI 已在合成 case-b 追加确认，扫描进度变为 `2/72`。2026-09-17 用户确认卡片下一轮（约五秒内）自动更新，另两案保持 `1/72` 和 `0/0`。此项以用户现场验收为依据。
- 双轴代码审查：Standards 无发现；Spec 无代码缺陷，宿主更新验收缺口已闭合。
- 验收结束后恢复原卡片设置，保留已安装的实现包。

#2 验收完成；首版其余执行票仍按各自范围验收。

## #3 离线依赖验收记录

- 使用首选的随包方案，没有远程 import 退路，不需要改动另一个仓库的 ADR。
- 2026-09-17，Windows Zebar 3.3.1：退出宿主，以全新 WebView 用户数据目录冷启动。
  WebView 设置不可用的外部代理 `http://127.0.0.1:9`，仅 localhost、127.0.0.1、[::1] 绕过代理，
  以此阻断外部网络、保留宿主本机通信；没有断开整台机器的网络。
- 使用 #2 留存的三个种子回放工作区；真实宿主 DOM 与扫描 CLI 输出核对，案件数为 3，
  进度为 `1/72`、`2/72`、`0/0`，当前模块、下一个与待看数全部一致，页面没有报错。
- 通过本机 CDP 读取冷启动资源记录（37 条）；开启网络记录、禁用并清空浏览器缓存再重载，
  观察 12 秒（覆盖两轮轮询），再手动扫描。重载后记录 28 条请求，没有外部地址请求、没有加载失败，
  三行保持一致，扫描时刻自动更新，手动按钮也正常。白名单中的 16 个客户端模块实际加载成功。
- 本次不增加新的单测缝：扫描逻辑未改，离线与资源加载在真实宿主上验收；Python 3.9.25 的
  全套 11 条 unittest、编译检查、依赖闭包与哈希核对、`git diff --check` 通过。
- 验收后恢复原卡片设置和正常宿主启动，保留已安装的本地依赖包。临时截图、网络记录与合成数据不提交。
- 双轴审查：Standards 无发现；Spec 保留一项验收缺口——上述代理隔离验证不等于整机断网。
  #3 的「拔网线（或断开网络）重启宿主」仍待现场验证，因此尚不能将该票标为全部验收完成。
- 2026-09-17，补做整机断网冷启动：备份原设置后切至上述三个合成案件，使用每次新建的 WebView 缓存启动脚本，不设置代理。用户按断网、重启、核对三案内容与刷新、恢复网络的步骤验收后确认「显示正确」。由此闭合此前的整机断网验收缺口，#3 验收完成。
- 本次复核 Python 3.9.25 的 17 条 unittest、编译检查，以及 16 个随包模块的 SHA-256、相对导入闭包、白名单与 ASCII 文件名，全部通过。实现未改，沿用此前双轴审查；验收后恢复原设置并按字节哈希核对。

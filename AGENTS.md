# loo0ng-widget

常驻律师桌面的一张卡片，同时看住在办的多个破产案件。它是一个 Zebar 组件包，不是独立应用，也不是 Claude Code 插件。裁定在 loo0ng-skills 的 `docs/adr/0029-*.md`（含 2026-09-16 附注）；这个仓库的规格与票在 GitHub Issues 上。

## 硬边界（不上桌）

1. **真实案件材料永不入本仓库**：喂料一律是合成的（单测用手写在 `tests/` 下的最小视图，端到端用 loo0ng-skills `evals/种子/` 回放出来的工作区），当事人写甲乙丙。案件事实、案号、当事人名不得写进代码、注释、issue、commit message。本仓库没有机械守门，这一条靠写的人自己守。
2. **卡片只读，一个字节都不写**：不写工作区、不写图、不写派生视图。唯一的例外是记在 WebView 本地存储里的**呈现偏好**（窗口位置、停在哪一页哪一案、选中的形式，ADR-0002），它不碰文件、不碰工作区，也不新开通往引擎的写入面。

## 结构不变量

1. **只读一个工作区文件**：卡片只读每个工作区的 `图视图.json`。`材料/`、`文书/`、`待归档/`、`参考/` 一律不读、不列、不显示。**源码里只许出现这一个工作区文件名**，由一条测试守。这是 ADR-0029 拿来替代「写一条豁免条款」的那条可被检查的限制。
2. **只聚合，不判定**：卡片显示的每个值，要么直接取自引擎算好的字段，要么是对那些字段的计数、排序、取第一个。任何需要看**条目**才能得出的结论，必须引擎先算好再交过来。**输出的任何层级都不含条目**，由一条测试守。
3. **格式版本只认 2**：遇到不认识的 `格式版本` 明确报错，不猜、不降级。错误按案件隔离，一个坏文件不许拖垮整张卡片。
4. **一条缝**：随包脚本的命令行是这个仓库唯一的测试缝（扫描脚本一个人做完发现、读、聚合、显示串格式化；唤起脚本只找宿主、调一条命令）。**页面只渲染，不含任何算法**（不计数、不排序、不取第一个、不算日期）。这个仓库不引入 node，不引入 JS 测试跑器。
5. **随包脚本的底线是 python 3.9**：律师那台 mac 的 `/usr/bin/python3` 是 3.9.6。测试要在 3.9 上真跑，不是只按形状扫。
6. **`includeFiles` 是资源服务器的白名单，不是附带清单**：`htmlPath` 指的文件没被它覆盖，宿主直接回 500，窗口里是一张没有别的线索的报错页。加文件必须同时加进白名单。包里的文件名保持 ASCII（中文名没验过）。
7. **不动 loo0ng-skills**：不新增 skill、不动引擎、不加字段、不升格式版本。`图视图.json` 的格式说明住在 skill 包里，受「随包自足」约束，那边不许出现本仓库的名字或链接；接口与消费方的关系记在 ADR-0029。

## 长约定指针

- issue 操作（本仓 GitHub Issues，用 `gh`）：[docs/agents/issue-tracker.md](./docs/agents/issue-tracker.md)
- 分诊标签（五角色）：[docs/agents/triage-labels.md](./docs/agents/triage-labels.md)
- 词汇：共享词用 loo0ng-skills 的 `CONTEXT.md` 那一套（节点、模块、图、派生视图、前方、时限、条目、高亮、空白模板、起手、预设图、工作区）；卡片自己的词（清单页、案件页、模块视、矩阵视、唤起、呈现偏好、材质）在本仓库 [CONTEXT.md](./CONTEXT.md)。「桌面卡片」指这个东西，「插件」今后只指 Claude Code 插件（ADR-0029）。
- 本仓库的裁定：[docs/adr/](./docs/adr/)，编号从 0001 起，与 loo0ng-skills 的不混；引用那边的写「ADR-0029」这种全称时以那边为准。
- 接口：`图视图.json` 的字段见 loo0ng-skills 的 `skills/engineering/graph/GRAPH-FORMAT.md`。

## 分支

`main` 是实现。`prototype/卡片` 是原型分支，答完题留着当 primary source，**不删、不动**；实现在主线上长，不从原型分支合过来。

## 本文件写什么

每轮整篇进上下文，只装三种东西：硬边界；违反了产品就坏的结构不变量（每条一行，理由在 ADR-0029）；指向长约定的一行指针。对模型行为的要求不写在这里。
